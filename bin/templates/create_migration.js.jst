import { BaseMigration } from 'supersonic'

export default class {{= (data.migrationName + '_migration').classify() }} extends BaseMigration {
  constructor() {
    super()
  }

  change() {
    return this.createTable(
      { {{ for (var key in data.structure) { }}
        {{= '' + key.toString() + ': "' + data.structure[key] + '",' }} {{ } }}
      }
    )
  }
}
